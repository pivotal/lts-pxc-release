#!/bin/bash

set -eo pipefail

CONFIG_PATH=/var/vcap/jobs/lf-agent/config/lf-agent.yml \
  /var/vcap/packages/configure-leader-follower/bin/configure-leader-follower \
  configure-leader-follower 2>&1 | tee -a  /var/vcap/sys/log/mysql/configure-leader-follower.log
